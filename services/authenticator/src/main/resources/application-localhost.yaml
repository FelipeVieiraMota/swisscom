spring:
  boot:
    admin:
      client:
        url: ${SPRING_BOOT_ADMIN_CLIENT_URL:http://localhost:10000}

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/authenticator}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}

app:
  bootstrap-admin:
    email: ${BOOTSTRAP_ADMIN_EMAIL:admin@swisscom.local}
    password: ${BOOTSTRAP_ADMIN_PASSWORD:ChangeMe-Admin-2026!}
